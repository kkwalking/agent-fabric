# AgentFabric 下一阶段功能需求

请基于已经完成的 AgentFabric MVP 继续扩展项目能力。

现有 MVP 已经具备基础的：

* Provider 管理
* Model 管理
* Agent Runtime 管理
* OpenCode Runtime
* Pi Agent Runtime
* Docker 容器执行
* Task / Run
* 基础 Session / Logs / Events / Artifacts
* Secrets
* Usage / Cost
* CLI
* API
* 基础 Web UI

本阶段不要重新设计已有 MVP，而是在现有基础上进一步明确 **Runtime 生命周期、Agent Harness 会话连续性、Handoff，以及 Workspace** 的产品模型。

AgentFabric 的整体定位仍然是：

> **Run any agent, on any model, in any environment.**

同时需要坚持一个核心原则：

> AgentFabric 不试图统一不同 Agent Harness 的内部状态和 Session 格式，而是负责它们的运行、生命周期管理以及不同 Harness 之间的工作交接。

---

## 1. Runtime Container 生命周期

Agent Runtime 运行于独立容器中。

Runtime Container 不应该默认作为长期状态存储，而应该被视为一个可替换的执行环境。

默认生命周期采用：

**Run 级 Ephemeral Container**

流程概念如下：

Task 提交后创建 Run，AgentFabric 为该 Run 创建 Runtime Container，挂载 Workspace，执行 Agent Task，保存执行结果和必要状态，然后销毁 Container。

Container 默认是 disposable 的。

需要支持以下生命周期策略：

### Ephemeral

默认模式。

每次 Run 创建新的 Runtime Container。

Run 完成、失败、取消或超时后销毁 Container。

适用于绝大多数自动化 Agent Task。

### Session Keep-Alive

允许 Runtime Container 在一次 Run 完成后短时间继续保留。

如果用户很快继续同一个 Harness 的任务，可以复用现有 Runtime Container。

需要支持 Idle Timeout。

超过 Idle Timeout 后自动停止并销毁 Runtime Container。

该模式主要用于交互式 Coding Agent 场景。

### Persistent

架构上允许未来支持长期运行的 Runtime Container。

Persistent Runtime 适用于长期 Agent、Daemon Agent、Event-driven Agent 等场景。

Persistent Runtime 不属于当前阶段的核心实现目标，只需要在生命周期模型上保留扩展空间。

---

## 2. Container 与状态解耦

AgentFabric 需要明确：

**Container 不是长期状态的唯一载体。**

Runtime Container 可以被销毁和重新创建。

以下内容不能因为 Container 被销毁而自动丢失：

* Task
* Run History
* Workspace
* Artifacts
* Logs
* Events
* Handoff
* Runtime Session Reference（如果 Harness 支持）

因此整体设计需要遵循：

> **Containers are disposable.**

Runtime Container 是执行载体，而不是 AgentFabric 的长期数据模型。

---

## 3. Harness Native Session

不同 Agent Harness 可以拥有自己的原生 Session 能力。

例如：

* Pi Agent Session
* OpenCode Session
* 未来其他 Agent Harness Session

AgentFabric 不应该试图定义统一的 Session 数据结构。

对于同一个 Agent Harness 的连续执行，应尽可能使用 Harness 自己提供的 Native Session Resume 能力。

例如：

Pi → Pi

优先恢复 Pi 自己的 Session。

OpenCode → OpenCode

优先恢复 OpenCode 自己的 Session。

AgentFabric 只需要保存对应的 Runtime Session Reference 和必要 Metadata。

AgentFabric 不需要理解 Harness Session 内部的数据结构。

---

## 4. Resume 与 Handoff

AgentFabric 需要明确区分两个概念：

### Resume

当用户继续使用相同 Agent Harness 时，使用该 Harness 的 Native Session 能力继续任务。

例如：

Pi → Pi

或者：

OpenCode → OpenCode

这种情况属于 Resume。

### Handoff

当用户将正在进行的 Task 从一个 Agent Harness 切换到另一个 Agent Harness 时，不进行原生 Session 转换，而是执行 Handoff。

例如：

Pi → OpenCode

OpenCode → Pi

这种情况属于 Handoff。

AgentFabric 不应该尝试把 Pi Session 转换成 OpenCode Session，也不应该定义一套统一 Session Schema。

新的 Agent Harness 会创建自己的新 Native Session。

---

## 5. Handoff

Handoff 是 AgentFabric 的一等概念。

Handoff 表示：

> 一个 Agent Harness 将当前 Task 的工作状态和必要上下文交接给另一个 Agent Harness。

Handoff 的目标不是复制前一个 Agent 的内部思考状态，而是让新的 Agent 能够理解：

* 当前任务是什么
* 前一个 Agent 已经做了什么
* 当前进展是什么
* 还有什么没有完成
* 有哪些关键决策
* 用户有哪些重要约束
* 当前 Workspace 是什么状态
* 有哪些重要文件或 Artifact
* 测试或构建目前是什么状态
* 下一步建议做什么

Handoff 本质上是语义性的工作交接，而不是 Session State Migration。

---

## 6. Handoff 内容

Handoff 可以包含以下信息：

* Original Task
* Current Objective
* Progress Summary
* Completed Work
* Remaining Work
* Important Decisions
* User Constraints
* Relevant Files
* Workspace Status
* Artifacts
* Test / Build Status
* Previous Run Result
* Notes for Next Agent

不要求所有字段必须存在。

Handoff 应允许不同类型的 Agent Task 根据实际情况生成不同内容。

---

## 7. Handoff 生成方式

AgentFabric 应支持多种 Handoff 来源。

### Harness-generated Handoff

如果当前 Agent Harness 本身能够生成高质量的工作总结，可以直接使用 Harness 输出的 Handoff。

### AgentFabric-assisted Handoff

如果 Harness 没有原生 Handoff 能力，AgentFabric 可以基于：

* 当前 Task
* Run Result
* Agent Messages
* Workspace 状态
* 文件变化
* Artifacts
* Logs
* Test Results

生成一份适合下一个 Agent 使用的 Handoff。

### User-provided Handoff

用户可以在切换 Harness 时补充额外说明。

例如：

“切换到 OpenCode，继续修剩下的两个测试，不要修改现有 API。”

这些用户说明应成为 Handoff 的一部分。

---

## 8. Handoff 与 Run 的关系

一个 Task 可以包含多个 Run。

不同 Run 之间可以存在 Handoff。

例如：

Task

→ Run #1 / Pi

→ Handoff

→ Run #2 / OpenCode

→ Run #3 / OpenCode

→ Handoff

→ Run #4 / Pi

因此 Handoff 应记录基本来源关系：

* From Run
* From Runtime
* To Runtime
* Created Time
* Handoff Content
* Related Workspace
* Related Artifacts

Handoff 应能够被用户查看和追踪。

---

## 9. Runtime Session 不作为 AgentFabric 顶层抽象

AgentFabric 不需要建立统一的跨 Runtime Session。

Runtime Session 只作为某个 Run 或某个 Harness 的私有运行状态存在。

AgentFabric 可以保存类似：

* Runtime Type
* Runtime Version
* Native Session Reference
* Resume Capability
* Runtime-specific Metadata

但不负责将不同 Harness 的 Session 相互转换。

整体原则是：

> **Same Harness → Resume**

> **Different Harness → Handoff**

---

## 10. Workspace

Workspace 在 AgentFabric 中提升为顶层抽象概念。

Workspace 是 Agent 执行任务时使用的长期工作环境，也是不同 Runtime 和不同 Run 之间共享工作成果的重要载体。

尤其对于 Coding Agent：

> Workspace 是 Agent 工作结果和任务上下文的重要来源。

当前阶段只需要建立 Workspace 的基础抽象，不需要过早设计复杂的 Snapshot、Fork、Diff、Lock、Remote Storage 等高级能力。

这些能力留待后续版本继续讨论。

---

## 11. Workspace 基础能力

当前阶段 Workspace 至少需要支持：

### Create

创建新的 Workspace。

Workspace 可以代表一个新的工作目录或项目环境。

### Import

将已有工作内容导入 AgentFabric Workspace。

例如：

* Existing Local Project
* Git Repository
* Existing Directory
* Existing Project Environment

具体 Storage Backend 不需要在本阶段做复杂抽象。

### Attach

Runtime Container 在执行 Run 时可以 Attach 到指定 Workspace。

Container 可以被销毁，但 Workspace 应独立存在。

新的 Runtime Container 可以重新 Attach 同一个 Workspace。

这使得不同 Run、不同 Runtime 能继续操作同一份工作成果。

### Save

Run 执行完成后，需要确保 Workspace 中 Agent 产生的有效修改得到保存。

Container 被销毁不应该导致 Workspace 修改丢失。

---

## 12. Workspace 与 Runtime 的关系

Workspace 不属于某个 Runtime。

例如同一个 Workspace 可以依次被：

* Pi Runtime
* OpenCode Runtime
* Pi Runtime
* 其他 Runtime

使用。

因此 Workspace 必须保持 Runtime-neutral。

Runtime 自己的临时数据、缓存和内部状态不应该成为 Workspace 的核心定义。

Workspace 主要用于保存用户真正关心的工作内容。

---

## 13. Workspace 与 Task 的关系

Task 可以关联一个 Workspace。

一个 Workspace 也可以被多个 Task 使用。

不要强制定义：

Task owns Workspace。

更合理的是：

Task references Workspace。

Task 完成后，Workspace 不应该自动销毁。

Workspace 应拥有独立生命周期。

---

## 14. Workspace 与 Handoff

跨 Harness Handoff 时，Workspace 是最重要的连续性来源之一。

例如：

Pi Runtime 完成部分工作后：

* 保存 Workspace
* 生成 Handoff
* Pi Runtime Container 销毁

随后：

* 创建 OpenCode Runtime Container
* Attach 同一个 Workspace
* 将 Handoff 提供给 OpenCode
* 创建新的 OpenCode Native Session
* OpenCode 继续执行

因此跨 Runtime 连续任务的基本模型是：

> **Workspace + Handoff**

而不是跨 Runtime Session Migration。

---

## 15. Task 的长期执行模型

一个 Task 可以长期存在，并经历多个不同 Agent Harness。

例如：

Task：实现 Payment Refund 功能

Run #1：

* Runtime: Pi
* Workspace: payment-service

Handoff #1

Run #2：

* Runtime: OpenCode
* Workspace: payment-service

Run #3：

* Runtime: OpenCode
* Workspace: payment-service

Handoff #2

Run #4：

* Runtime: Pi
* Workspace: payment-service

从用户视角，这仍然是同一个 Task。

从 AgentFabric 视角，它由多个 Run、多个 Native Runtime Session 和多个 Handoff 组成。

---

## 16. Run 模型调整

Run 仍然代表一次具体 Agent 执行。

Run 需要能够关联：

* Task
* Runtime
* Model
* Provider
* Workspace
* Runtime Container
* Runtime Session Reference
* Input Instruction
* Status
* Logs
* Events
* Artifacts
* Usage
* Previous Handoff
* Generated Handoff

Run 不需要拥有长期 Workspace。

Run 只是 Attach Workspace 并对其执行操作。

---

## 17. Runtime Capability

不同 Agent Harness 能力并不完全相同。

AgentFabric 应允许 Runtime 声明自身能力。

例如：

* Supports Native Session
* Supports Native Resume
* Supports Streaming Events
* Supports Handoff Generation
* Supports Workspace
* Supports Interactive Execution

AgentFabric 根据 Runtime Capability 决定可以提供哪些行为。

例如：

如果 Runtime 支持 Native Resume，则同 Harness 继续执行时优先 Resume。

如果不支持 Native Resume，则可以创建新 Session，并通过已有 Task Context / Handoff 继续执行。

---

## 18. 用户切换 Runtime

用户在继续 Task 时可以选择更换 Runtime。

例如：

当前 Task：

Runtime: Pi

用户可以选择：

Continue with Pi

或者：

Switch to OpenCode

如果继续 Pi：

使用 Native Resume。

如果切换到 OpenCode：

执行 Handoff，创建新的 OpenCode Runtime 和 OpenCode Native Session，并继续使用当前 Workspace。

用户界面应该明确告诉用户当前行为是：

Resume

还是：

Handoff。

不要给用户造成“不同 Harness 的 Session 被无损迁移”的错觉。

---

## 19. AgentFabric 核心抽象

经过本阶段调整后，AgentFabric 的主要顶层概念可以保持为：

* Provider
* Model
* Runtime
* Task
* Run
* Workspace
* Handoff
* Artifact

Runtime Native Session 属于 Runtime-specific state，不作为 AgentFabric 的统一顶层 Session 模型。

其中：

Task 表示长期目标。

Run 表示一次执行。

Workspace 表示持续存在的工作环境和工作成果。

Handoff 表示不同 Agent Harness 之间的任务交接。

Runtime 表示实际执行任务的 Agent Harness。

Container 表示 Runtime 的临时执行载体。

---

## 20. 本阶段核心流程

本阶段需要重点支持以下流程：

### 同 Harness 继续任务

Task

→ Existing Workspace

→ Existing Harness

→ Native Resume

→ New Run

→ Save Workspace

→ Preserve Runtime Session Reference

→ Container 根据生命周期策略保留或销毁

### 跨 Harness 继续任务

Task

→ Existing Workspace

→ Previous Harness

→ Generate Handoff

→ New Harness

→ New Native Session

→ Attach Existing Workspace

→ Inject Handoff

→ New Run

→ Save Workspace

→ Preserve New Runtime Session Reference

→ Container 根据生命周期策略处理

---

## 21. 当前阶段暂不展开的 Workspace 能力

Workspace 当前只保留顶层抽象和基本能力：

* Create
* Import
* Attach
* Save

以下能力暂时不要在本阶段深入设计：

* Snapshot
* Fork
* Branch
* Diff
* Rollback
* Locking
* Concurrent Write
* Advanced Storage Backend
* Remote Workspace
* Versioning
* Automatic Git Management
* Workspace Merge

这些能力后续单独讨论和设计。

当前阶段重点是确保 Workspace 已经成为稳定的一等资源，使未来可以在该抽象之上继续扩展。

---

## 22. 本阶段设计原则

整个实现和产品设计需要遵循以下原则：

**1. Containers are disposable.**

Container 是执行环境，不是长期状态本身。

**2. Workspace is durable.**

Runtime 和 Container 可以变化，但 Workspace 中的工作成果应持续存在。

**3. Harness sessions stay native.**

不要建立统一的 AgentFabric Session Schema。

**4. Same Harness means Resume.**

优先利用 Harness 自己的 Native Session 能力。

**5. Different Harness means Handoff.**

不同 Harness 之间通过 Handoff 交接任务，而不是转换 Session。

**6. Workspace + Handoff provide cross-runtime continuity.**

跨 Agent Harness 的任务连续性主要依赖 Workspace 和 Handoff。

**7. AgentFabric orchestrates execution, not agent cognition.**

AgentFabric 不应该试图统一不同 Agent Harness 的内部推理模型、内部 Memory、Todo、Tool Call 或 Session 数据结构。

---

本阶段的目标，是在已经完成的 AgentFabric MVP 上建立更加稳定的长期任务执行模型：

> **Task 可以跨多个 Run 持续存在，Workspace 保存工作成果，同 Harness 使用 Native Resume，不同 Harness 通过 Handoff 完成交接，而 Runtime Container 根据执行需要动态创建和销毁。**

这应该成为 AgentFabric 后续继续扩展 Multi-Agent、Workflow、Workspace Management 和长期 Agent Execution 的基础。
